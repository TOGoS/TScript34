# TScript35-P0023 : Test cases and simple interpreter(s) to prototype a simple Scheme-like language

To test the cases with Chicken Scheme:

```cmd
cat test-cases.tsv | C:\tools\chicken\bin\csi.exe -ss src\interp01\chicken\run-tests.scm
```

Or whatever works on your platform.
